android {
            defaultConfig {
                applicationId = "org.nativescript.HelloWorld"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }